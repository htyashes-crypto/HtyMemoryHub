export default function ModuleView(_p: {
  name: string | null;
  onOpenModule: (n: string) => void;
  onOpenImpact: (t: string) => void;
  onOpenDoc: (n: string) => void;
}) {
  return <div>module stub</div>;
}
